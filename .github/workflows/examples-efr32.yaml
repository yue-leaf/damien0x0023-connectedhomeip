# Copyright (c) 2020 Project CHIP Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Build example - EFR32

on:
  push:
    branches:
      - huwei-ci

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}-${{ (github.event_name == 'pull_request' && github.event.number) || (github.event_name == 'workflow_dispatch' && github.run_number) || github.sha }}
  cancel-in-progress: true

env:
  CHIP_NO_LOG_TIMESTAMPS: true
  snbAccessKey: ${{ secrets.IOT_ACCESS_KEY }}
  snbSecretKey: ${{ secrets.IOT_SECRET_KEY }}
  snbSignDataKey: ${{ secrets.IOT_SIGN_DATA_KEY_ID }}

jobs:
  efr32:
    name: EFR32

    env:
      SILABS_BOARD: BRD2703A
      BUILD_TYPE: gn_efr32

    runs-on: ubuntu-latest
    if: github.actor != 'restyled-io[bot]'

    container:
      image: ghcr.io/project-chip/chip-build-efr32:94
      volumes:
        - "/tmp/bloat_reports:/tmp/bloat_reports"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout submodules & Bootstrap
        uses: ./.github/actions/checkout-submodules-and-bootstrap
        with:
          platform: silabs_docker

      - name: Set up environment for size reports
        uses: ./.github/actions/setup-size-reports
        if: ${{ !env.ACT }}
        with:
          gh-context: ${{ toJson(github) }}

#      - name: Test SLC gen
#        timeout-minutes: 30
#        run: |
#          scripts/examples/gn_silabs_example.sh examples/lighting-app/silabs ./out/light-app BRD2703A  --slc_generate --docker
#          echo "=========================1======================"
#          ls out/light-app/
#          echo "=========================2======================"
#
#      - name: Publish artifacts 1
#        uses: actions/upload-artifact@v4
#        with:
#          name: light-app,${{ github.event_name }}
#          path: |
#            out/light-app/*
#
#      - name: clean out build output
#        run: rm -rf ./out

      - name: Build light
        run: |
          ./scripts/run_in_build_env.sh \
             "./scripts/build/build_examples.py \
                --enable-flashbundle \
                --target efr32-brd2703a-light-skip-rps-generation \
                build \
                --copy-artifacts-to out/artifacts \
             "
          echo "========================1====================="
          ls out/artifacts/efr32-brd2703a-light-skip-rps-generation
          ls out/artifacts/efr32-brd2703a-light-skip-rps-generation/flashbundle/
          cp out/artifacts/efr32-brd2703a-light-skip-rps-generation/flashbundle/matter-silabs-lighting-example.s37 \
            ./silabs-lighting.s37

      - name: Set up commander and snb-command
        run: |
          DEBIAN_FRONTEND=noninteractive apt install -y unzip libkrb5-3 libgssapi-krb5-2 libglib2.0-0 libgl1 wget bzip2 && \
          wget https://www.silabs.com/documents/public/software/SimplicityCommander-Linux.zip && \
          unzip SimplicityCommander-Linux.zip && \
          tar -xjf SimplicityCommander-Linux/Commander_linux_x86_64_1v17p0b1771.tar.bz  && \
          ln -s $(pwd)/commander/commander /usr/local/bin/commander && \
          commander --help
          wget https://github.com/yue-leaf/cli-test/releases/download/v0.0.1-alpha/snb && chmod +x snb && \
          ln -s $(pwd)/snb /usr/local/bin/snb

      - name: Signature data
        run: |
          commander convert silabs-lighting.s37 --secureboot --extsign --outfile silabs-lighting && \
          snb kms sign -k $snbSignDataKey -f ./silabs-lighting.extsign -o ./silabs-lighting-signature.der && \
          snb kms exportKey -t public-key -k $snbSignDataKey -o ./public_key.pem && \
          commander convert silabs-lighting.s37 --secureboot --signature silabs-lighting-signature.der --verify public_key.pem --outfile silabs-lighting.s37 && \
          commander util appinfo silabs-lighting.s37

      - name: Publish artifacts
        id: upload_artifact
        uses: actions/upload-artifact@v4
        with:
          name: light-examples,${{ github.event_name }}
          path: |
            silabs-lighting.s37

      - name: Notify artifact download URL
        run: |
          DOWNLOAD_URL="${{ steps.upload_artifact.outputs.artifact-url }}"
          echo "Artifact download URL: $DOWNLOAD_URL"
          ./third_party/snb-iot/snb iot notify -u $DOWNLOAD_URL

  
