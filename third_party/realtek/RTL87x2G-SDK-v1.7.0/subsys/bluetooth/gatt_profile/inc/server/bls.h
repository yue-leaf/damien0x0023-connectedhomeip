/*
 * Copyright (c) 2026, Realtek Semiconductor Corporation
 *
 * SPDX-License-Identifier: LicenseRef-Realtek-5-Clause
 */

#ifndef _BLS_H_
#define _BLS_H_

#ifdef  __cplusplus
extern "C" {
#endif      /* __cplusplus */

/* Add Includes here */
#include "profile_server.h"
#include "bls_define.h"


/** @defgroup BLS  Blood Pressure Service
  * @brief  Blood Pressure Service
   * @details

     The Blood Pressure Service exposes blood pressure and other data related to a noninvasive blood pressure monitor
     for consumer and professional healthcare applications.

     Blood Pressure Service generally makes up a profile with some other services, and it can provide the state of blood pressure.

     The default supported feature provided by BLS is the indicate property of the Blood Pressure Measurement characteristic,
     the read property of the Blood Pressure Feature characteristic, and application developers can modify the feature supported
     by BLS according to their own requirements.
     The specific configuration process can be achieved by modifying the file @ref bls_define.h.

    * <b>Example usage</b>
    * \code{.c}

         #define BLS_INTERMEDIATE_CUFF_PRESSURE_SUPPORT    1

    * \endcode

     Applications shall register the Blood Pressure Service during initialization through the @ref bls_add_service function.

     Applications can set the blood pressure feature of BLS through the @ref bls_set_parameter function.

     Applications can send the blood pressure measurement value of BLS to the client with an indication through the @ref bls_blood_pressure_measurement_value_indicate function.

     Applications can send the intermediate cuff pressure value of BLS to the client with a notification through the @ref bls_intermediate_cuff_pressure_value_notify function.

  * @{
  */

/*============================================================================*
 *                         Macros
 *
 *============================================================================*/
/** @defgroup BLS_Exported_Macros BLS Exported Macros
  * @brief
  * @{
  */
#define  BLS_INDICATE_BLOOD_PRESSURE_MEASUREMENT_ENABLE   1  /**< Blood Pressure Measurement characteristic indication enabled index. */
#define  BLS_INDICATE_BLOOD_PRESSURE_MEASUREMENT_DISABLE 2   /**< Blood Pressure Measurement characteristic indication disabled index. */
#define  BLS_NOTIFY_INTERMEDIATE_CUFF_PRESSURE_ENABLE 3      /**< Intermediate Cuff Pressure characteristic notification enabled index. */
#define  BLS_NOTIFY_INTERMEDIATE_CUFF_PRESSURE_DISABLE 4     /**< Intermediate Cuff Pressure characteristic notification disabled index. */

/** End of BLS_Exported_Macros
* @}
*/

/*============================================================================*
 *                         Types
 *============================================================================*/
/** @defgroup BLS_Exported_Types BLS Exported Types
  * @brief
  * @{
  */

/**
*  @brief Blood Pressure Service parameter type
*/
typedef enum
{
    BLS_PARAM_BLOOD_PRESUREE_FEATURE  /**< Parameter type for @ref bls_set_parameter to set Blood Pressure Feature
                                           characteristic Flags field value.
                                           Use the combination of macros defined in @ref Blood_Pressure_Feature_Bit. */
} T_BLS_PARAM_TYPE;

/**
*  @brief Blood Pressure Service parameter upstream message data
*/
typedef union
{
    uint8_t notification_indification_index;
} T_BLS_UPSTREAM_MSG_DATA;

/**
*  @brief Set blood pressure parameter upstream callback data
*/
typedef struct
{
    uint8_t                 conn_id;
    T_SERVICE_CALLBACK_TYPE msg_type;
    T_BLS_UPSTREAM_MSG_DATA msg_data;
} T_BLS_CALLBACK_DATA;

/** End of BLS_Exported_Types
* @}
*/


/*============================================================================*
 *                         Functions
 *============================================================================*/
/** @defgroup BLS_Exported_Functions BLS Exported Functions
  * @brief
  * @{
  */

/**
  * @brief       Add Blood Pressure Service to the Bluetooth Host.
  *
  *
  * @param[in]   p_func  Callback when service attribute was read, write or CCCD update.
  * @return Service ID generated by the Bluetooth Host: @ref T_SERVER_ID.
  * @retval 0xFF Operation failure.
  * @retval Others Service ID assigned by Bluetooth Host.
  *
  * <b>Example usage</b>
  * \code{.c}
     void profile_init()
     {
         server_init(service_num);
         bls_id = bls_add_service(app_handle_profile_message);
     }
  * \endcode
  */
T_SERVER_ID bls_add_service(void *p_func);

/**
 * @brief       Set a Blood Pressure Service parameter.
 *
 * This function can be called with a BLS parameter type @ref T_BLS_PARAM_TYPE and it will set the BLS parameter.
 * The 'p_value' field must point to an appropriate data type that meets the requirements for the corresponding parameter type.
 * (For example: if required data length for parameter type is 1 octets, p_value should be cast to a pointer of uint8_t.)
 *
 * @param[in]   param_type   Blood Pressure Service parameter type: @ref T_BLS_PARAM_TYPE.
 * @param[in]   length       Length of data to write.
 * @param[in]   p_value      Pointer to data to write.
 *
 * @return Operation result.
 * @retval true Operation success.
 * @retval false Operation failure.
 *
 * <b>Example usage</b>
 * \code{.c}
    void test(void)
    {
        uint16_t blood_pressure_feature = BLS_FEATURE_BODY_MOVEMENT_DETECTION_SUPPORT_BIT |
                                          BLS_FEATURE_CUFF_FIT_DETECTION_SUPPORT_BIT;
        bls_set_parameter(BLS_PARAM_BLOOD_PRESUREE_FEATURE, 2, &blood_pressure_feature);
    }
 * \endcode
 */
bool bls_set_parameter(T_BLS_PARAM_TYPE param_type, uint8_t length, void *p_value);


/**
 * @brief       Send blood pressure measurement value indication data.
 *
 *
 * @param[in]   conn_id        Connection ID.
 * @param[in]   service_id     Service ID.
 * @param[in]   p_data         Pointer to data to indicate.
                               Type is org.bluetooth.characteristic.blood_pressure_measurement.
 * @return Operation result.
 * @retval true Operation success.
 * @retval false Operation failure.
 *
 * <b>Example usage</b>
 * \code{.c}
    void test(void)
    {
        uint8_t conn_id = 0;
        T_BLOOD_PRESSURE_MEASURMENT bls_meas_value = {0};
        SFLOAT bls_measure_pulse_rate = {0x30, 0x00};

        bls_meas_value.bp_meas_flag = (BLS_FLAG_MEASUREMENT_UINT_BIT |
                                       BLS_FLAG_MEASUREMENT_TIMESTAMP_PRESENT_BIT |
                                       BLS_FLAG_MEASUREMENT_PULSE_RATE_BIT |
                                       BLS_FLAG_MEASUREMENT_USER_ID_BIT |
                                       BLS_FLAG_MEASUREMENT_STATUS_BIT);
        bls_meas_value.time_stamp = (T_BLS_TIME_STAMP) {2017, 9, 20, 20, 6, 8};
        memcpy(&bls_meas_value.bp_meas_pulse_rate, &bls_measure_pulse_rate, 2);
        bls_meas_value.bp_meas_user_id = 0;
        bls_meas_value.bp_meas_status = (T_BLOOD_PRESSUREE_MEAS_STATUS) {0};

        bls_blood_pressure_measurement_value_indicate(conn_id, bls_id, &bls_meas_value);
    }
 * \endcode
 */
bool bls_blood_pressure_measurement_value_indicate(uint8_t conn_id, T_SERVER_ID service_id,
                                                   T_BLOOD_PRESSURE_MEASURMENT *p_data);


#if BLS_INTERMEDIATE_CUFF_PRESSURE_SUPPORT
/**
 * @brief       Send intermediate cuff pressure value notification data.
 *
 *
 * @param[in]   conn_id        Connection ID.
 * @param[in]   service_id     Service ID.
 * @param[in]   p_data         The current charge level of a blood pressure.
                               Type is org.bluetooth.characteristic.blood_pressure_measurement.
 * @return Operation result.
 * @retval true Operation success.
 * @retval false Operation failure.
 *
 * <b>Example usage</b>
 * \code{.c}
    void test(void)
    {
         T_BLOOD_PRESSURE_MEASURMENT bls_meas_value = {0};
        SFLOAT bls_measure_pulse_rate = {0x30, 0x00};

        bls_meas_value.bp_meas_flag = (BLS_FLAG_MEASUREMENT_UINT_BIT |
                                       BLS_FLAG_MEASUREMENT_TIMESTAMP_PRESENT_BIT |
                                       BLS_FLAG_MEASUREMENT_PULSE_RATE_BIT |
                                       BLS_FLAG_MEASUREMENT_USER_ID_BIT |
                                       BLS_FLAG_MEASUREMENT_STATUS_BIT);
        bls_meas_value.time_stamp = (T_BLS_TIME_STAMP) {2017, 9, 20, 20, 6, 8};
        memcpy(&bls_meas_value.bp_meas_pulse_rate, &bls_measure_pulse_rate, 2);
        bls_meas_value.bp_meas_user_id = 0;
        bls_meas_value.bp_meas_status = (T_BLOOD_PRESSUREE_MEAS_STATUS) {0};

        bls_intermediate_cuff_pressure_value_notify(conn_id, bls_id, &bls_meas_value);
    }
 * \endcode
 */
bool bls_intermediate_cuff_pressure_value_notify(uint8_t conn_id, T_SERVER_ID service_id,
                                                 T_BLOOD_PRESSURE_MEASURMENT *p_data);
#endif

/** @} End of BLS_Exported_Functions */

/** @} End of BLS */


#ifdef  __cplusplus
}
#endif      /*  __cplusplus */

#endif  /* _BLS_H_ */
